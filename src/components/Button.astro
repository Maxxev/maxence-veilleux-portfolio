---
import { Icon } from 'astro-icon/components';

const {
  link = false,
  outline = false,
  glow = false,
  RootTag = 'button',
  bgColor = 'var(--primary)',
  bgColorHover = 'var(--white)',
  // Texte FONCÉ sur l'aplat orange, et non blanc. Un orange assez vif pour
  // être la couleur du site n'atteint jamais 4,5:1 avec du blanc ; il y
  // arrive largement avec `--on-primary`. Voir _colors.scss.
  color = 'var(--on-primary)',
  iconName,
  reverseIcon = false,
  class: className = '',
  ...rest
} = Astro.props;
---

<RootTag
  class={`button${outline ? ' button--outline' : ''}${glow ? ' button--glow' : ''}${link ? ' button--link' : ''} ${className}`}
  style={{
    '--bgColor': bgColor,
    '--color': color,
    '--bgColorHover': bgColorHover,
  }}
  {...rest}
>
  <span class="content">
    <slot />
  </span>

  {
    iconName && (
      <Icon
        name={iconName}
        class="icon content"
        style={reverseIcon ? { order: -1 } : undefined}
      />
    )
  }
</RootTag>

<style lang="scss">
  @use '../styles/button';
</style>
