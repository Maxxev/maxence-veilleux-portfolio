---
const { RootTag = 'div', class: className = '', ...rest } = Astro.props;
---

<RootTag class={`div-block ${className}`} {...rest}><slot /></RootTag>

<style>
  @layer astro-components {
    .div-block {
      width: 100%;
      display: flex;
      flex-direction: column;
    }
  }
</style>
